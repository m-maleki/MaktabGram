<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"] - پنل مدیریت مکتب‌گرام</title>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Vazirmatn Font -->
    <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
    
    <!-- Site RTL Styles -->
    <link rel="stylesheet" href="~/css/site-rtl.css" asp-append-version="true" />
    
    <!-- Chart.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.2/dist/chart.umd.min.js"></script>
    
    <style>
        /* استایل‌های اختصاصی پنل ادمین */
        body {
            background: linear-gradient(to bottom right, #F9FAFB, #F3F4F6);
        }
        
        /* سایدبار مدرن */
        .sidebar {
            background: linear-gradient(to bottom, #312E81, #4C1D95);
            box-shadow: 4px 0 20px rgba(0, 0, 0, 0.15);
        }
        
        .sidebar a {
            transition: all 0.2s ease;
        }
        
        .sidebar a:hover {
            background-color: rgba(139, 92, 246, 0.3);
            transform: translateX(-2px);
        }
        
        .sidebar a.active-nav {
            background-color: rgba(139, 92, 246, 0.5);
            box-shadow: 0 4px 12px rgba(139, 92, 246, 0.3);
        }
        
        /* انیمیشن کارت‌های آمار */
        .stat-card {
            transition: all 0.3s ease;
        }
        
        .stat-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.15);
        }
        
        /* برای سایدبار موبایل */
    </style>
</head>
<body>
    <div class="flex h-screen overflow-hidden">
        <!-- سایدبار مدرن -->
        <aside id="sidebar" class="sidebar fixed z-30 right-0 top-0 h-full w-64 text-white md:translate-x-0 transform transition-transform duration-300 translate-x-full">
            <!-- هدر سایدبار -->
            <div class="flex items-center justify-between p-4 border-b border-indigo-700">
                <div class="flex items-center">
                    <div class="w-10 h-10 bg-gradient-to-br from-indigo-500 to-purple-500 rounded-lg flex items-center justify-center ml-3">
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z"></path>
                        </svg>
                    </div>
                    <div>
                        <h1 class="text-lg font-bold">پنل مدیریت</h1>
                        <p class="text-xs text-indigo-300">مکتب‌گرام</p>
                    </div>
                </div>
                <button id="close-sidebar" class="md:hidden text-xl p-1 hover:bg-indigo-700 rounded transition">&times;</button>
            </div>
            
            <!-- منوی ناوبری -->
            <nav class="p-4 space-y-1 mt-4">
                <a href="/Admin" class="flex items-center p-3 rounded-xl hover:bg-indigo-700 transition-all duration-200">
                    <svg class="w-5 h-5 ml-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
                    </svg>
                    <span>داشبورد</span>
                </a>
                
                <a asp-area="Admin" asp-page="/Users/Index" class="flex items-center p-3 rounded-xl hover:bg-indigo-700 transition-all duration-200">
                    <svg class="w-5 h-5 ml-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path>
                    </svg>
                    <span>مدیریت کاربران</span>
                </a>
                
                <a href="#" class="flex items-center p-3 rounded-xl hover:bg-indigo-700 transition-all duration-200">
                    <svg class="w-5 h-5 ml-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                    </svg>
                    <span>مدیریت پست‌ها</span>
                </a>
                
                <a href="#" class="flex items-center p-3 rounded-xl hover:bg-indigo-700 transition-all duration-200">
                    <svg class="w-5 h-5 ml-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 3.055A9.001 9.001 0 1020.945 13H11V3.055z"></path>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.488 9H15V3.512A9.025 9.025 0 0120.488 9z"></path>
                    </svg>
                    <span>گزارشات و نمودارها</span>
                </a>
                
                <a href="#" class="flex items-center p-3 rounded-xl hover:bg-indigo-700 transition-all duration-200">
                    <svg class="w-5 h-5 ml-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37a1.724 1.724 0 002.572-1.065z"></path>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                    </svg>
                    <span>تنظیمات</span>
                </a>
            </nav>
            
            <!-- اطلاعات اضافی در پایین سایدبار -->
            <div class="absolute bottom-4 w-full px-4">
                <div class="bg-indigo-700 rounded-xl p-4 border border-indigo-600">
                    <p class="text-sm font-medium mb-1">وضعیت سیستم</p>
                    <p class="text-xs text-indigo-200 mb-2">همه چیز عالی است ✓</p>
                    <div class="w-full bg-indigo-800 rounded-full h-2">
                        <div class="bg-gradient-to-r from-green-400 to-green-500 h-2 rounded-full" style="width: 85%"></div>
                    </div>
                </div>
            </div>
        </aside>

        <!-- محتوای اصلی -->
        <div class="flex-1 flex flex-col overflow-hidden md:mr-64">
            <!-- هدر -->
            <header class="bg-white shadow-sm border-b border-gray-200 px-6 py-4">
                <div class="flex items-center justify-between">
                    <div class="flex items-center">
                        <button id="open-sidebar" class="md:hidden text-gray-500 hover:text-gray-700 ml-3">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path>
                            </svg>
                        </button>
                        <h2 class="text-2xl font-bold text-gray-800">@ViewData["PageTitle"]</h2>
                    </div>
                    
                    <div class="flex items-center gap-4">
                        <!-- نوتیفیکیشن -->
                        <button class="relative p-2 text-gray-500 hover:bg-gray-100 rounded-lg transition">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"></path>
                            </svg>
                            <span class="absolute top-1 left-1 w-2 h-2 bg-red-500 rounded-full"></span>
                        </button>
                        
                        <!-- منوی کاربری -->
                        <div class="relative" id="user-menu-container">
                            <button id="user-menu-button" onclick="toggleUserMenu()" class="flex items-center gap-2">
                                <div class="w-10 h-10 bg-gradient-to-br from-indigo-500 to-purple-500 rounded-full flex items-center justify-center text-white font-bold shadow-lg">
                                    <span>م</span>
                                </div>
                                <div class="hidden md:block text-right mr-3">
                                    <p class="text-sm font-semibold text-gray-800">مدیر سیستم</p>
                                    <p class="text-xs text-gray-500">ادمین</p>
                                </div>
                                <svg class="w-5 h-5 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                                </svg>
                            </button>
                            
                            <!-- Dropdown منو -->
                            <div id="user-menu" class="hidden absolute left-0 mt-2 w-56 bg-white rounded-lg shadow-xl border border-gray-200 py-2 z-50">
                                <a href="#" class="flex items-center px-4 py-2 hover:bg-gray-100 transition">
                                    <svg class="w-5 h-5 ml-3 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                                    </svg>
                                    <span class="text-sm">پروفایل</span>
                                </a>
                                <a href="#" class="flex items-center px-4 py-2 hover:bg-gray-100 transition">
                                    <svg class="w-5 h-5 ml-3 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37a1.724 1.724 0 002.572-1.065z"></path>
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                                    </svg>
                                    <span class="text-sm">تنظیمات</span>
                                </a>
                                <hr class="my-2">
                                <a href="/Account/Logout" class="flex items-center px-4 py-2 hover:bg-red-50 text-red-600 transition">
                                    <svg class="w-5 h-5 ml-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
                                    </svg>
                                    <span class="text-sm">خروج</span>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </header>

            <!-- محتوای صفحه -->
            @RenderBody()
        </div>
    </div>

    <!-- Scripts -->
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Chart.js initialization if elements exist
            const postsChartEl = document.getElementById('postsChart');
            const usersChartEl = document.getElementById('usersChart');

            if (postsChartEl) {
                const postsCtx = postsChartEl.getContext('2d');
                new Chart(postsCtx, {
                    type: 'line',
                    data: {
                        labels: ['فروردین', 'اردیبهشت', 'خرداد', 'تیر', 'مرداد', 'شهریور'],
                        datasets: [{
                            label: 'تعداد پست‌ها',
                            data: [12, 19, 3, 5, 2, 3],
                            borderColor: 'rgb(107, 70, 193)',
                            backgroundColor: 'rgba(107, 70, 193, 0.1)',
                            tension: 0.4
                        }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        plugins: {
                            legend: {
                                display: true,
                                position: 'top',
                                labels: {
                                    font: {
                                        family: 'Vazirmatn'
                                    }
                                }
                            }
                        },
                        scales: {
                            y: {
                                beginAtZero: true
                            }
                        }
                    }
                });
            }

            if (usersChartEl) {
                // Chart 2: Users Chart
                const usersCtx = usersChartEl.getContext('2d');
                new Chart(usersCtx, {
                    type: 'bar',
                    data: {
                        labels: ['هفته ۱', 'هفته ۲', 'هفته ۳', 'هفته ۴'],
                        datasets: [{
                            label: 'کاربران ثبت نامی',
                            data: [350, 480, 220, 600],
                            backgroundColor: [
                                'rgba(255, 99, 132, 0.6)',
                                'rgba(54, 162, 235, 0.6)',
                                'rgba(255, 206, 86, 0.6)',
                                'rgba(75, 192, 192, 0.6)'
                            ],
                            borderColor: [
                                'rgba(255, 99, 132, 1)',
                                'rgba(54, 162, 235, 1)',
                                'rgba(255, 206, 86, 1)',
                                'rgba(75, 192, 192, 1)'
                            ],
                            borderWidth: 1
                        }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        plugins: {
                            legend: {
                                display: true,
                                position: 'top',
                                labels: {
                                    font: {
                                        family: 'Vazirmatn'
                                    }
                                }
                            }
                        },
                        scales: {
                            y: {
                                beginAtZero: true
                            }
                        }
                    }
                });
            }
        });

        // === Table Search/Filter Logic ===
        function filterTable() {
            const input = document.getElementById("user-search");
            const filter = input.value.toUpperCase();
            const tableBody = document.getElementById("user-table-body");
            const rows = tableBody.getElementsByTagName("tr");

            // Define a function to convert Farsi/Arabic digits to English for consistent search
            function normalizeDigits(str) {
                if (typeof str !== 'string') return str;
                return str.replace(/[۰-۹]/g, d => '۰۱۲۳۴۵۶۷۸۹'.indexOf(d).toString())
                         .replace(/[٠-٩]/g, d => '٠١٢٣٤٥٦٧٨٩'.indexOf(d).toString());
            }

            for (let i = 0; i < rows.length; i++) {
                const cells = rows[i].getElementsByTagName("td");
                let match = false;

                // Loop through all cells
                for (let j = 0; j < cells.length; j++) {
                    const cell = cells[j];
                    if (cell) {
                        // Normalize both cell text and filter input for better search matching
                        const cellText = normalizeDigits(cell.textContent || cell.innerText);
                        const normalizedFilter = normalizeDigits(filter);

                        // Check if the cell content contains the filter text
                        if (cellText.toUpperCase().indexOf(normalizedFilter) > -1) {
                            match = true;
                            break; // Found a match in this row
                        }
                    }
                }

                // Show or hide the row based on the match result
                if (match) {
                    rows[i].style.display = "";
                } else {
                    rows[i].style.display = "none";
                }
            }
        }


        // === Mobile Sidebar Toggle Logic ===
        const sidebar = document.getElementById('sidebar');
        const openBtn = document.getElementById('open-sidebar');
        const closeBtn = document.getElementById('close-sidebar');

        openBtn.addEventListener('click', () => {
            sidebar.classList.add('open');
        });

        closeBtn.addEventListener('click', () => {
            sidebar.classList.remove('open');
        });

        // Close sidebar when clicking outside on mobile
        document.querySelector('main').addEventListener('click', (e) => {
            if (sidebar.classList.contains('open') && window.innerWidth < 768) {
                // Check if the click is outside the sidebar area
                if (!sidebar.contains(e.target) && e.target !== openBtn) {
                     sidebar.classList.remove('open');
                }
            }
        });
    </script>

</body>
</html>
